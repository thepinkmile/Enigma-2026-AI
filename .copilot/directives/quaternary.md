# QUATERNARY DIRECTIVE — File Deletion

⛔ **NEVER permanently delete any file from the repository.**

If a file must be removed, **move it to `.recycle-bin/`** at the repo root and inform the user.
Permanent deletion is a user-only action.

`.recycle-bin/` is listed in `.gitignore` — its contents are never committed.
